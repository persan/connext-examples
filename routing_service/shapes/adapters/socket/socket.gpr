with "dds-c.gpr";
with "dds.gpr";
project socket is

   for Source_Dirs use ("src");
   for Object_Dir use ".obj";
   for Languages use ("C");
   for Library_Name use "socket";
   for Library_Dir use "lib";
   for Library_Kind use "dynamic";
   package Compiler renames Dds.Compiler;
end socket;

