with "dds-ada.gpr";
with "dds.gpr";
library project Dds.Ada.Routing is

   for Languages use ("Ada");
   for Library_Name use "DDSroutingAda";
   for Library_Version use "lib" & project'Library_Name & "." & DDS.Version & ".so";
   for Source_Dirs use ("src/**");
   for Object_Dir use ".obj";
   for Library_Dir use "lib";
   for Library_Kind use "relocatable";

end Dds.Ada.Routing;

